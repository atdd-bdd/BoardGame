Options +ExecCGI
AddHandler cgi-script .cgi

RewriteEngine On

# Pass all requests to app.cgi
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ /app.cgi/$1 [QSA,L]

# Also handle root
RewriteRule ^$ /app.cgi [QSA,L]
